; Production inventory for dts-cryptobot.fr
; VPS_HOST is injected by GitHub Actions (or exported locally)
[vps]
prod ansible_host=${VPS_HOST} ansible_user=ubuntu

[vps:vars]
ansible_python_interpreter=/usr/bin/python3
ansible_ssh_common_args=-o StrictHostKeyChecking=accept-new -o ConnectTimeout=30
